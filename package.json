{
  "name": "lexiflood",
  "private": true,
  "scripts": {
    "dev:api": "npm --prefix api run dev",
    "dev:front": "npm --prefix front run dev",
    "dev:all": "npm-run-all --parallel dev:api dev:front"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
